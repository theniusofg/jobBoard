name: Fetch Google Sheets Data

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight

jobs:
  fetch-sheets-data:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'
    
    - name: Install Dependencies
      run: |
        npm init -y
        npm install googleapis
    
    - name: Fetch Google Sheets Data
      env:
        GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
      run: |
        node << EOF
const { GoogleAuth } = require('googleapis/build/src/auth/googleauth');
const { google } = require('googleapis');
const fs = require('fs');

async function fetchSheetsData() {
  const auth = new GoogleAuth({
    credentials: JSON.parse(process.env.GOOGLE_SERVICE_ACCOUNT_KEY),
    scopes: ['https://www.googleapis.com/auth/spreadsheets.readonly']
  });

   const sheets = google.sheets({ version: 'v4', auth });
  
  const SPREADSHEET_ID = '1pYihXbRT8heKDOqbl8MxGZxlzeA6ka_TyeazM-0Tts4';
  
  const response = await sheets.spreadsheets.values.get({
    spreadsheetId: SPREADSHEET_ID,
    //range: 'Sheet1!A1:Z'  // Adjust sheet name if different
  });

  // Write to JSON file
  fs.writeFileSync('sheets_data.json', JSON.stringify(response.data.values));
  console.log('Data saved to sheets_data.json');
}

fetchSheetsData().catch(console.error);
EOF